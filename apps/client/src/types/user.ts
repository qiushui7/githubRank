export interface UserInfo {
  name: string;
  login: string;
  avatar_url: string;
  country_code: string;
  location: string;
  bio: string;
  score: number;
  totalStars?: number;
  followers: number;
}
